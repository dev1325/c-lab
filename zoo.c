#include<stdio.h>
#include<string.h>

int main()
{
  int i,j=0;
  char str[20];
  scanf("%s",str);
 
  j=strlen(str);
  for(i=0;str[i]!='o';i++)
  {
  }

  if(i*2==(j-i))
  {
      printf("Yes");
  }
  else
  {
      printf("No");
  }
}